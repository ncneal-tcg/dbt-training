version: 2

sources:
  - name: dbt_nneal
    description: nick neal's dev schema

    tables:
      - name: stripe_payment
        loaded_at_field: _batched_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}
        description: raw payments data.
        columns:
          - name: id
            description: unique payment id.
            tests:
              - unique
              - not_null